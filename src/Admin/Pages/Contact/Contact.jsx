import React from 'react';
import { Typography } from 'antd'

function Contact(props) {
    const {Title} = Typography
    return (
        <>
            <Title type="success" level={2}>Contact</Title>
        </>
    );
}

export default Contact;